import * as jwt from 'jsonwebtoken';
import { Config } from './config';
import { BaseFilter } from './filter';
export declare class BlackList {
    private config;
    private filter;
    constructor(config?: Config);
    add(token: string, options?: jwt.DecodeOptions): Promise<boolean>;
    has(token: string, options?: jwt.DecodeOptions): Promise<boolean>;
    clear(): Promise<boolean>;
    getFilter(): BaseFilter;
}
export declare function createBlackList(config?: Config): Promise<BlackList>;
